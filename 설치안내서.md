# 디자인사 아카이브 — 운영 안내서

## 매주 해야 할 일
1. Supabase 대시보드 → Table Editor → `weeks` 테이블에서 해당 주차의 `is_open`을 `true`로 변경
2. 이전 주차는 `is_open`을 `false`로 바꿔도 되고, 그대로 둬도 학생 홈에는 "열려 있는 주차 중 가장 큰 번호"만 보입니다

## 교수자 계정 승격
Supabase 대시보드 → SQL Editor에서 아래 실행 (학번은 본인 것으로):
```sql
update public.profiles set role='professor' where student_no='0000';
```

## 관리자 화면
로그인 후 주소창에 `/admin` 입력
