# 디자인사 수업자료 — 작업 규칙

> 이 레포에는 두 갈래 작업이 있다. **수업 슬라이드 덱**(이 문서)과 **수업 허브 앱**(`memory.md`).
> 슬라이드 작업을 이어받는 세션은 이 문서를 먼저 읽을 것.

## 이 수업이 뭔지

홍익대학교 디자인컨버전스학부 1학년 **「디자인사」**, 2026-2학기. 담당 박지혜.
**실기 전공생이 듣는 이론 수업**이라, 텍스트보다 **시각자료가 핵심**이다.
수강생은 **선행 이론과목이 없다** — 미술사를 들은 학생도 있지만 안 들은 학생이 더 많고,
디자인사는 대부분 처음이다. 르네상스·바로크 같은 양식 이름을 이미 아는 말로 전제하지 말 것.
주차당 100분, 15주. 주교재는 Fiell & Fiell 『디자인의 역사』.

## 시작하기 전에 (2026-09 기준)

작업 폴더는 **`~/design-history-app`** 이다. 임시폴더가 아니라 영구 클론이다.

**다른 세션이 같은 폴더에서 허브 앱을 작업 중이다.** 슬라이드(`public/slides/`)와 앱 코드는
겹치지 않지만, 상대가 `git add -A` 를 돌리면 내 미커밋 변경이 그쪽 커밋에 딸려 들어간다.
**작업 단위가 끝나면 바로 커밋**하고, 푸시 전에 `git pull` 한다.

`node tools/slides/verify.js` 전체 검증은 **3분 이상** 걸린다. 명령을 `&&` 로 길게 엮으면
타임아웃에 걸려 뒤의 커밋이 통째로 날아간다. 검증과 커밋은 따로 실행할 것.

## 파일 구조

| 경로 | 내용 |
|---|---|
| `public/slides/index.html` | 덱 전체. 단일 HTML, 프레임워크 없음 |
| `public/slides/img/*.webp` | 이미지. 외부 파일(base64 인라인 아님) |
| `tools/slides/fetchimg.py` | 커먼즈 이미지 수집 → 라이선스 검증 → WebP 변환 |
| `tools/slides/verify.js` | **배포 전 필수 검증** |
| `tools/slides/img_credits.tsv` | 이미지 출처·라이선스 기록 |

덱은 허브 앱이 `<iframe src="/slides/index.html?week=N">`으로 같은 오리진에서 띄운다.
`?week=N` 주차 지정 · `?p=N` 슬라이드 지정(주차 내 0-index) · `?clean=1` UI 숨김.

## 글쓰기 규칙

- **`~하다`체.** 존댓말 금지. 예외는 인용문 안(원문 말투 유지).
- **AI 티 나는 문장 금지.** 화려한 전환 문구, 과한 em dash, "~라는 점에서 의미가 있다" 류.
- 1학년이 읽는 글이다. 짧고 직접적으로.
- 각 슬라이드는 `<h3>` 하나 + 불릿 3~5개가 기본. THINK 블록은 토론거리가 있을 때만.

**슬라이드는 읽는 글이 아니라 띄워놓고 보는 글이다(2026-09-09 정리).**
그런데 **학생에게 교재가 없다 — 이 덱이 곧 시험 범위다.** 두 조건이 같이 걸린다.
그래서 원칙은 **"사실은 남기고 서술만 줄인다"** 이지 "줄인다"가 아니다.
3주차를 이 기준으로 훑어 최장 불릿 178자 → 100자, 장당 평균 216자로 맞췄다.

- **사실·이름·연도·수치는 화면에 남긴다.** 뺄 것은 접속구와 되풀이다 —
  "~인 셈이다", "동시에 …", "그런데 화면이 친절하게 …", "~라는 점에서 …사례다".
- **한 불릿 = 한 생각. 100자를 넘기지 않는다.** 문장 서넛을 이어붙이면 그건 문단이다.
- **나란한 것은 줄을 가른다.** 대비가 문장에 묻히면 안 된다.
  예: "아름다움은 …이고 숭고는 …이다" 한 줄 → 「아름다움 —」 「숭고 —」 두 줄.
  프랑스 1791 · 영국 1814 도 각각 한 줄. 병렬 구조 자체가 암기를 돕는다.
- **연도는 문장 안이 아니라 줄 앞에 세운다.** `1807` 폴몰 가스등, `1812` 가스회사.
- **결론을 em dash 로 덧붙이지 말고 독립 불릿으로 뺀다.**
- **화면에 '교재'라는 말을 쓰지 않는다(2026-09-11).** "교재는 그를 …라 부른다", "교재의 평가는
  냉정하다" 같은 출처 표시를 4·5·6주차에서 전부 걷어냈다. **학생에게 교재가 없으니** 화면이
  볼 수 없는 책을 가리키면 안 된다. "…로 평가된다", "평가는 냉정하다"로 주어를 지운다.
  예외는 1주차 「교재 및 참고문헌」(수업 안내용 도서 목록)과 `#course-data` JSON 이다.
- **교재 표현을 임의로 다듬지 않는다(2026-09-19, 교수자 지시).** 2주차 40번에서 교재의 「공공연하게 여성적이고 화려한 로코코양식」을 내가 「가볍고 화려한 로코코」로 바꿨다가 **교수자 지시로 되돌렸다.** 표현이 지금 기준으로 낡아 보여도 **교재 표현 중심으로 간다.** 고치고 싶으면 먼저 묻는다. **다만 「최초」를 한정하는 손질은 계속한다** — 그건 교수자가 2026-09-12 에 직접 시킨 별개의 규칙이다.
- **교재와 사실이 어긋나면 원칙은 교재를 따른다(2026-09-11).** 다만 **교재 쪽이 명백한 오기이면
  근거가 가장 확실한 쪽을 따른다(2026-09-19, 교수자가 판단을 맡김).** 대박람회 관람객은 교재가
  "유료방문객 60만 명 이상"이지만 공식 집계가 6,039,195명이고 1실링 표만 약 450만 장이라
  **교재의 자릿수 오기로 보고 600만으로 되돌렸다**(108번). 「잉글랜드·웨일스 인구 약 1,790만의
  3분의 1」 문장도 연인원이라는 한정을 달아 다시 넣었다. 외국인 관람객 수는 출처가 없어 넣지 않았다.
- **한자 병기 금지.** 신(新)·쾌(快)·도판(圖版) 전부 지웠다.

  **인명은 원어를 함께 적는다(2026-09-12, 교수자 지시).** 「조사이어 웨지우드 Josiah Wedgwood」,
  「카스파르 다비드 프리드리히 Caspar David Friedrich」처럼 한글 뒤에 원어를 붙인다.
  **사람 이름에만 해당한다** — 용어·문헌명·기관명의 원어 병기는 여전히 최소로 하고
  (steam bending, Statute of Monopolies 같은 것은 대본으로), 인명 원어에 괄호는 쓰지 않는다.
- **수업 순서가 아니라 시대로 말한다.** "2주차에서 본", "앞 장에서 본", "곧 볼",
  "오늘 다룰"은 쓰지 않는다. 대신 **260만 년 전 주먹도끼**, **1760년대 웨지우드 공장**,
  **19세기 후반 모리스**처럼 시점을 적는다. 슬라이드 한 장이 혼자서도 읽혀야 한다.

  **「N주차」는 수업 내용에 한 번도 쓰지 않는다(2026-09-12, 교수자 지시).** 줄 끝의
  `(→ 5주차)` 표시도 마찬가지다 — 전에 "덱 전체 관례라 남겨뒀다"고 적어뒀던 것을
  뒤집는다. **예외는 셋뿐이다**: 1주차 오리엔테이션의 강의계획 안내, 중간·기말고사
  슬라이드의 시험 범위 표기(「1~7주차」), 그리고 `#course-data` JSON. 내용을 말하는
  자리에서는 전부 시대로 바꾼다.

  고칠 때 두 갈래다. **68·75·88번처럼 이미 시대를 적어두고 뒤에 괄호만 덧붙인 경우는
  괄호만 뗀다**(「19세기 후반 모리스가 … (→ 5주차)」 → 괄호 삭제). **76·85·89번처럼
  주차가 유일한 시점 표시인 경우는 시대를 새로 적는다.** 67번이 그 예다 —
  「러스킨과 모리스의 산업 생산 비판 (→ 5주차)」를 「19세기 중반 러스킨과 19세기 후반
  모리스의 산업 생산 비판이 여기서 출발」로 바꿨다.

  **남은 곳은 덱 전체에 33군데다(예외 제외, 2026-09-12 기준).** 2주차 2 · 3주차 6 ·
  4주차 5 · 5주차 5 · 6주차 3 · 7주차 1 · 9주차 1 · 10주차 2 · 11주차 2 · 12주차 3 ·
  13주차 1 · 14주차 2. 세는 법은 `data-week` 로 장을 가르고 본문에서 `\d+\s*주차` 를
  찾으면 된다.
- **「최초」·「처음」을 함부로 쓰지 않는다(2026-09-12, 교수자 지적).** 교수자가 "웨지우드가 최초가
  맞나"라고 물어 확인한 결과, 디자인과 제조의 분리는 고블랭(1660년대)·마이센의 모델러 캔들러(1731)·
  세브르·리옹 견직물 도안가가 앞선다. **무엇의 최초인지 한정하거나 범위를 좁힌다** —
  「처음으로 분리해 관리」 → 「**도자기 산업에서 본격적으로** 분리해 관리」,
  「최초의 디자인 경영」 → 「**최초의 근대적 마케팅**」(이쪽은 매켄드릭 1960 이후 통용되는 평가라 살렸다).
  같은 손질을 74·93번에도 했다. **「…으로 꼽힘」처럼 남의 평가임을 밝힌 꼴은 그대로 둬도 된다.**
- **화면에서 뺀 사실은 버리지 말고 대본으로 내린다.** 부차적 인명·연도·원어가 그렇다.
- **볼드는 한 불릿에 하나.** 셋에 하나가 볼드면 아무것도 강조되지 않는다. 고를 때는
  **연도·수치·핵심 용어** 순이고, 나열의 첫 항목만 굵히거나(「영국 **조지왕조**, 스웨덴 구스타비안」)
  대비의 앞쪽만 굵히지(「**옛 방식**에서 새 방식으로」) 않는다. **문장 전체를 볼드로 감싸지 않는다** —
  불릿 자체가 요점이면 볼드 없이 둔다. 3주차 기준: 불릿당 0.75개, 글자의 17%.

**문체 — 번역체 걷어내기(2026-09-11~12 정리).** 교수자가 "기계 번역체 같다"고 지적해
3주차 62·63번을 시험 삼아 고쳤고, 그 결과가 기준이 됐다. **다음 다섯이다.**

1. **직역만 푼다. 학술 용어는 남긴다.** 「사랑을 부른다」(excites love),
   「두려움 섞인 경외를 부른다」(inspires awe), 「핵심은 ~라는 점이다」(the key point is),
   「~하는 통로를 열었다」(opened the way for) 는 영어 문장이 그대로 남은 자리다.
   반면 **「미적 경험」·「미적 대상」 같은 개념어는 건드리지 않는다.**
   구어로 내려가면 개념이 흐려진다 — 62번을 「겁이 나는데 눈을 못 뗀다」로 바꿨다가
   숭고가 감상 소감이 돼버려 되돌렸다. **정확함이 자연스러움보다 앞선다.**
2. **제목은 묘사가 아니라 주장으로.** 「보름달 밤에 모인 사람들」 →
   「산업혁명은 서로 아는 사람들에게서 나왔다」. 덱의 좋은 제목들이 이미 그렇다
   (「선을 그은 것은 물건이 아니라 제도였다」, 「장식이 싸지면 사람들은 장식을 늘린다」).
3. **화면만 보고도 이유가 성립해야 한다.** 63번에 「달빛이 있어야 밤에 집에 갈 수 있었다」만
   있고 **가로등이 없었다는 말은 대본에만** 있었다. 교재가 없으니 근거가 화면에 없으면
   그냥 이상한 문장이 된다. **대본에만 있는 근거는 화면으로 올린다.**
4. **마지막 줄이 추상으로 끝나면 실물을 붙인다.** 「과학과 제조와 디자인이 아직 한 테이블에
   앉아 있었다」는 좋은 비유지만 시험에 쓸 게 없다. 뒤에 **증기기관·표준 몰드·'힘을 판다'는
   발상**을 붙여 앞으로 만날 것을 가리키게 했다.
5. **나란한 명단에 괄호를 반복하지 않는다.** 「볼턴(제조업자) · 와트(증기기관) · …」 →
   「볼턴 제조업자 · 와트 증기기관 · …」. 다섯 번 반복되면 읽는 속도만 떨어진다.

**3주차를 2026-09-12 에 한 번 더 훑었다(둘째 판).** 교수자가 51·52·54·55번 화면 문안과
**대본 49~97번 전 장**을 자기 판으로 보내왔고 그대로 반영했다. 덱에서 바뀐 것은
51·52·54·55·62(버크 정체)·63(과장 수정)·65(「최초」 좁힘)·66(링크)·71(THINK)·77(THINK)·
87(네 줄→두 줄)·88(중복 두 줄 삭제)·94(휨가공·링크)번과 **주차 타이틀 12장의 기간 연도**,
그리고 **뉴커먼 1705년**(교재를 따름)이다. 대본 PDF 는 75쪽이 됐다(60쪽에서 늘었다).

**교수자 판의 방향이 일정했다 — 단정을 눅이는 쪽이다.** 「~였습니다」를 「~이기도 했습니다」로,
「막을 수 없었던 겁니다」를 「그 걱정은 현실이 됐습니다」로, 「최초의 기술입니다」를 빼고
무엇이 새로운지만 남긴다. **인과를 주장하지 말고 사실을 나란히 두는 것**이 기준이다
(「우연이 아니에요」 → 「배스커빌이 활동한 버밍엄은 볼턴과 와트가 활동하던 곳이기도 합니다」).

**첫째 판 기록: 3주차 45장 전부 교수자 확인을 받았다.** 교수자가 한 장씩
자기 판을 보내오면 그대로 넣는 방식으로 갔다. 마지막에 일관성 점검을 돌렸고
「N주차」 지칭·1851을 만국박람회로 부르는 곳·한자 병기·수업 순서 지칭·원어 없는 인명이
모두 0 이다. **대본 아티팩트와 PDF 도 이날 함께 맞췄다.**

**교수자 판이 매번 내 판보다 나았고, 차이가 일정하다.** 나는 덜어내는 쪽으로 기울었고
교수자는 **학생이 받아적을 것을 남기는 쪽**이었다. 몇 번이나 이렇게 갈렸다 —
54번에서 내가 뺀 백과전서·애덤 스미스를 되살리며 연도와 편찬자를 더했고,
55번에 「디자인의 분화」 항목을 새로 넣었고, 57번 빙켈만과 55번 바퇴에 정체를 달았다.
**줄일지 말지 갈리면 남긴다. 화면이 곧 시험 범위다.**

교수자 판에서 되풀이된 손질이 넷이다. 먼저 적용해두면 왕복이 준다.
① **「조건 → 결과」 꼴**로 쓴다(52번 석탄: 캐기 쉬웠지만 물이 찼다 → 퍼낼 기계가 필요했다).
② **처음 나오는 인명에 정체를 단다**(빙켈만: 고고학자·미술사학자 / 바퇴: 철학자, 1713~1780).
③ **여러 항목이 단계를 이루면 번호를 매긴다**(55번 1~4).
④ **맺음말은 두 줄** — 사실을 한 줄로 닫고 `→` 로 함의를 한 줄 더.

**verify.js 는 한 장 고칠 때마다 돌리지 말 것.** 2~3분이라 이것이 작업 속도의 주범이다.
글만 고치는 장은 **두세 장 모아 한 번** 돌리고, 표·2단·사진이 있어 넘칠 위험이 있는 장만
바로 확인한다.

**대본을 맞추는 법 — 손으로 옮겨 적지 말 것.** 2026-09-12 에 3주차 49장의 회색 칸을
전부 다시 맞췄다. **덱을 파싱해 회색 칸 HTML 을 생성하고 대본에 끼워 넣은 뒤, 49장을
기계로 대조해 일치를 확인**했다. 표·2단·연표·인용·THINK 블록은 각각 `연표` `2단`
`인용` `물음표` `사진` `브릿지` 표시로 옮긴다.

**말하는 문장에 슬라이드 번호를 쓰지 않는다(2026-09-12, 교수자 지시).** 「78번에서 다시 봅니다」가
아니라 「특허 이야기에서 다시 봅니다」, 「75번에서 그 그림을」이 아니라 「오늘 뒤에 볼 「콜브룩데일의
밤」이」로 **구체적 명사로 가리킨다.** 그날 아홉 군데를 고쳤다. **표식(회색 알약) 안의 번호는 남긴다** —
학생에게 읽는 말이 아니라 교수자가 PDF 에서 찾아가는 표시다. 다만 회수 대상이 분명하면 표식도
「폼페이 회수」·「숭고 회수」처럼 이름으로 쓰는 편이 낫다.

**회색 칸보다 말하는 부분이 더 잘 썩는다.** 그날 24곳을 고쳤는데 종류가 넷이다.
① **없어진 문장을 그대로 인용**하고 있던 곳(91·92·94·73번), ② **「N번째 줄」이 밀린**
곳(64·72·74·76·81번), ③ **다른 장을 가리키는 번호**(52번이 특허를 76번이라 했는데
78번이다), ④ **머리말·시간표·꼬리말의 집계와 목록** — 「미술사 열두 장」이 실제로는
열 장이었고, 시간표의 생략·보존 표시 다섯 곳과 꼬리말의 「자르는 순서」 여섯이 전부
한두 장씩 밀려 있었다. **덱을 고쳤으면 이 넷을 반드시 훑는다.**

**PDF 는 주차별로 최신본 한 개만 둔다(2026-09-14, 교수자 지시).** 예전에는 `_이전판_날짜.pdf` 로 복사해두고 덮어썼는데,
그 사본과 다운로드 폴더의 「문체수정본」들을 이날 전부 휴지통으로 옮겼다. **이제 이전판을 만들지 말고 그냥 덮어쓴다.**
대본 아티팩트도 주차당 하나다(오리엔테이션 4c1b2f33 · 2주차 274d5477 · 3주차 768d373f · 4주차 c1d10e85).
3주차는 60쪽이다(문안이 늘어 56쪽에서 늘었다).

**인수인계 — 2026-09-19 밤 기준(5주차 완료 후).**
- **화면 문체:** 2주차(9~49)·3주차(50~99)·4주차(100~137)·**5주차(138~173)** 요약 문체 완료. **6주차 이후(174~310)는 아직 옛 문체다.** 다음 작업은 6주차부터.
- **장 수는 310.** 5주차를 25장 → 36장으로 늘렸다. 6주차부터 전역 번호가 11 밀렸다(6주차 표지 163 → 174). 대본이 있는 1~4주차는 그대로다.
- **분량 기준은 주차당 100분이다(2026-09-19 교수자 확인).** 60분은 4주차에만 붙인 단축 옵션이다. 100분 주차는 장 수 38~50, 글자 12,000~18,000 이 기준이다. 옛 5주차는 25장 9,000자로 60분 분량이었다. **6·7주차(18장·15장)는 더 모자라니 교재로 채울 것.**
- **5주차에서 한 일(2026-09-19, 교수자가 「판단해서 쭉 가라」고 맡김).** 교재 6장(개혁의 바람)과 7장(뉴 아트) 순서를 따랐다 — 시대배경 → 퓨진 → 스트리트·버제스 → 비올레-르-뒤크 → 러스킨 → 라파엘전파 → 모리스(레드하우스·모리스사·패턴·서섹스 의자·벤슨·켈름스콧·정치·여성·모순) → 유미주의 3장 + 미술사 곁들임 「휘슬러의 공작의 방」 → 드레서 → 민족낭만주의 → 두 번째 국면(센추리 길드·애쉬비·리버티·녹스·보이시·짐슨) → 미국 2장 → 세계로 → 연표(172) → 브릿지. 이미지 29장 추가, 캐러셀 9곳. 옛 「필립 웹과 도메스틱 리바이벌」 장은 레드하우스·베드포드 파크 장으로 흡수해 없앴다.
- **교재와 맞춘 사실:** 레드하우스 1860 완공(옛 1859), 필립 **웨브**, 「모리스, 마셜, 포크너사」, 리버티 1875, 「미술공예」라는 이름은 1888 전시회에서 유래, 길드 오브 우먼 바인더스는 **제본**(옛 「제본·자수·도자」는 틀림). 교재에 「최초」가 쓰여 있으면 그대로 둔다(교수자 확인) — 142 「최초의 고건물보존기관」, 153 「최초의 디자인 개혁가」, 160 드레서.
- **교재 밖에서 넣은 것:** 러스킨 고딕 여섯 특징, 켈름스콧(골든체 1890·425부), 모리스의 사회주의 활동, 메이 모리스, 휘슬러 「공작의 방」과 1878 소송, 맥머도 『렌의 도시 교회들』(1883), 애쉬비 1888·1907, 리버티가 지금도 영업한다는 줄.
- **교재 7장은 아르누보가 아니다** — 민족낭만주의·영국 미술공예 두 번째 국면·미국 미술공예다. 아르누보는 **8장**이다. 바탕화면 「디자인의역사」에 8~20장 PDF 가 2026-09-19 에 들어왔다. 6주차의 매킨토시·티파니·라이트·설리번은 교재 7장 내용이지만 아르누보 주차에 두었다 — 5주차 「결국 기계를 받아들이다」에 매킨토시 한 줄만 있다.
- **교재 메모:** 6·7장을 읽고 요약한 것이 `~/design-history-backup/교재메모/` 에 있다(레포는 공개라 밖에 둔다). 스캔 PDF 는 가로·거꾸로 찍힌 쪽이 섞여 있어 `sips -r 90/180/270` 로 돌려 읽었다.
- **커먼즈 원본 다운로드가 막힐 때:** 큰 원본은 연달아 받으면 upload 서버가 거부한다. `iiurlwidth` 로 thumb URL 을 받아 내려받으면 된다(5주차에 쓴 `thumbget.py` 방식 — 스크래치패드에 있었으므로 필요하면 fetchimg.py 에 옵션으로 넣을 것).
- **대본 아티팩트는 다섯 개다** — 오리엔테이션 4c1b2f33 · 2주차 274d5477 · 3주차 768d373f · 4주차 c1d10e85 · **5주차 claude.ai/artifact/9hv5PiDgaSPK1kCuA95ZBM**(2026-09-19, 36장·100분 + 60분 시간표, PDF 67쪽). 5주차 회색 칸은 덱에서 기계로 뽑았다.
- **5주차 60분 단축 표식 11장**(2026-09-19, 교수자 요청): 141·142·144·152·153·154·158·159·166·170·171 — 교재 밖이거나 곁가지인 장을 골랐다. 남는 25장에 「60분이면」 한 문장씩 건너뛴 내용을 넣었다.
- **주의: 2주차·4주차 대본의 원본 HTML 은 그 세션 스크래치패드에만 있었다.** 세션이 끝나면 사라지니, 다음에 고칠 때는 **아티팩트를 `action:"read"` 로 읽어 되살린 뒤** 작업한다. 4주차는 `build_w4_script.py` 빌더로 다시 생성할 수도 있었지만 그 스크립트도 스크래치패드에 있었다.
- **2주차 대본은 회색 칸만 덱과 다시 맞췄다. 말하는 부분은 옛 화면 기준 그대로다** — 사실이 다 남아 있어 흐름은 맞지만, 다듬으려면 그 부분부터 보면 된다.
- **4주차에는 60분 단축용 표식 13장이 있다**(102·103·105·106·110·115·116·119·120·123·126·130·134). 대본에 60분 시간표가 따로 들어 있다.
- **지금도 살 수 있는 브랜드 연결**: 3주차 웨지우드·토넷, 4주차 싱어(132), 5주차 모리스 상회(149)·리버티(165). 6주차 이후에도 쓸 수 있으면 같은 방식으로.

**slide_overrides 는 비어 있다.** 3주차 50번에 있던 행(3,2)을 교수자가
`/admin/slides/3/2` 의 「원본으로 되돌리기」로 지웠다. 지우기 전 내용은
`~/design-history-backup/slide_override_3_2_2026-09-12.json` 에 있다.
**이제 파일이 곧 화면이다.** 다시 생기면 같은 문제가 되풀이되니 `/admin/slides` 에서
고친 것은 파일로 옮기고 오버라이드는 지운다. 애초에 그 화면의 편집은 **학생 화면에
반영되지 않는다** — 학생이 보는 덱은 `public/slides/index.html` 정적 파일이고
`slide_overrides` 를 읽는 곳은 관리자 화면과 verify·백업뿐이다.

**4주차부터의 방침(2026-09-14, 교수자 지시).** 3주차에서 굳힌 요약 스타일로 **문체를 전부 바꾼다.**
덱은 수업 페이지이지만 **수업용**이므로, 화면은 항목·연도·이름·→ 만 남기고 **필요한 서술은 대본에 넣는다.**
**필요한 이미지는 캐러셀로 넣는다.** 그리고 **어느 장이든 시대적 상황과의 연결, 그리고 디자인 사건·이슈를 다룬다** —
사건만 나열하지 말고 "그때 세상이 어땠길래 이 일이 생겼나"를 붙인다(3주차의 「1750년의 세계」·「한 세대에 겹친 세 개의 혁명」이 본보기).

**4주차에서 정한 것(2026-09-14).** ① 표지 다음에 시대배경 장 「1851년의 영국」을 새로 넣는다.
② 겹치는 장을 합친다 — 100(수정궁 요약)은 맨 뒤 정리 연표로, 103(디자인 개혁 요약)은 111·114로 흡수,
104(수정궁 사진)는 106 수정궁 공학의 캐러셀로, 105·116(연표 둘)은 한 장으로. **사실은 버리지 않고 대본으로 내린다.**
③ **블록별로 확인받는다** — 대박람회 → 디자인 개혁 → 미술사 곁들임 → 미국식 시스템 순.
블록 1(대박람회)은 2026-09-14 에 배포했다(지금 전역 100~109).
**블록 2~4도 2026-09-19 에 요약 문체로 바꿨다(110~135).** 이때는 **장을 합치거나 빼지 않았다** — 이미 4주차 대본(c1d10e85)과 표식 13장이 있어서 번호가 밀리면 전부 다시 맞춰야 하기 때문이다. 총 299장 그대로다. 겹치던 110번(디자인 개혁 요약)은 버리지 않고 **블록 도입 장 「물건은 늘었는데 기준이 없었다」로 바꿔** 겹침만 없앴다. 새로 넣은 이미지는 넷이다 — 117 퓨진 중세 궁정, 114 『장식의 문법』 무어 도판, 118 캐러셀(딕킨슨 전시 구역 · 오슬러 유리 분수). 존스 도판 1번(「미개 부족의 장식」)은 받았다가 **수업 화면에 쓰기 부적절해 파일과 tsv 줄을 지웠다.** **132번 싱어에 「이 회사는 지금도 가정용 재봉틀을 판다」 한 줄을 넣었다(교수자 제안)** — 웨지우드·토넷처럼 지금도 살 수 있는 브랜드를 잇는 줄이다. 민튼은 현재 판매 여부를 확인하지 못해 쓰지 않았고, 콜트·레밍턴은 지금은 총기 회사라 같은 방식으로 쓰지 않는다. 모리스 상회(Sanderson Design Group)는 5주차에 쓰면 된다. **2026-09-19 에 146번(패턴 디자이너 모리스)에 넣었다** — 「지금도 모리스 상회라는 이름으로 팔린다 — '딸기 도둑'과 '핌퍼넬'(1876)이 그대로 벽지와 직물이 된다」. 이로써 지금도 살 수 있는 브랜드가 주차마다 하나씩 생겼다 — 3주차 웨지우드·토넷, 4주차 싱어(132), 5주차 모리스 상회(146).
**4주차 끝 연표(136번)도 같은 날 넣었다** — 브릿지 앞에 옮겨두었던 옛 요약 장 「수정궁 — 산업이 스스로를 전시하다」를
그 자리에서 연표로 **바꿔 끼웠으므로 장 수가 그대로이고 뒤 주차 번호는 밀리지 않았다.** 옛 요약 장의 문장
(「할 수 있다고 해서 해야 하는가」·「기계는 반복이 빠르지만 좋은 형태를 보장하지 않는다」)은 4주차 대본을 쓸 때 옮긴다.
4주차 연표는 10년 칸 8개(1790~1810년대 … 1880~90년대)다. **블록 2~4에서 장을 고치면 연표의 사건·의의도 같이 확인할 것.** **2주차도 같은 방식으로 고친다(같은 날 교수자 지시)** —
2주차는 이미 수업을 마쳐 대본 아티팩트(274d5477)가 있으므로 덱을 고치면 대본도 같이 맞춘다.
**2주차 블록 2~4도 2026-09-19 에 요약 문체로 바꿨다(20~47).** 4주차와 같이 **장을 합치거나 빼지 않아 299장 그대로**다. 고친 장은 스물(21·23·25·26·27 / 29·30·31·33·35·36·38 / 39·40·41·42·43·44·45·47)이고, **큰 사진 장(22·24·28·32·34·37·46)과 카드 장(20)은 이미 요약 형식이라 손대지 않았다.** 긴 불릿을 쪼개고 **맺음말 두 줄을 스무 장에 새로 넣었다**(2주차에는 원래 블록 1에만 있었다). 한자 병기를 둘 지웠고(23번 「미(美)」, 30번 「관요(官窯)」), 인명 원어를 붙였다. 「최초」를 한정한 곳이 셋이다 — 41번 치펜데일·45번 셰라튼은 「이른 사례」로, 31번은 그대로 뒀다. **40번 로코코에서 교재의 「여성적이고 화려한」을 「가볍고 화려한」으로 바꿨다** — 양식을 성별로 규정하는 표현이라 뺐다. 교수자 확인이 필요하면 되돌린다. **44번은 사진 2단 비교에 맺음말을 더했더니 1280×800 에서 85%로 축소돼 verify 가 잡았다** — 불릿 넷을 둘로 합쳐 해소했다(0.94). 사진 2단·카드·연표가 있는 장에 맺음말을 더할 때는 불릿을 먼저 줄일 것.

**모든 주차 끝(브릿지 바로 앞)에 연대순 사건 타임라인 한 장을 둔다(2026-09-14, 교수자 지시).** **넣으면 그 뒤 모든 주차의 전역 번호가 한 칸씩 밀린다** — 이미 대본이 있는 주차(1·2·3주차)는 번호를 같이 고칠 것. 2주차 연표를 넣을 때 3주차 대본 번호를 +1 로 일괄 수정했다.
3주차 97번 「기술과 사상은 같은 시대에 나란히 움직였다」가 본보기다 — `wk-tl` 두 줄 연표(위: 기술·생산·디자인 /
아래: 사회·사상·미술), 연대 칸에 맞춰 같은 시기가 세로로 읽히게. **사건은 그 주차 슬라이드에 이미 나온 것만 쓴다.**
**사건마다 의의를 짧게, 단어로 붙인다(같은 날 교수자 지시)** — 문장이 아니라 「광산 배수」·「분업의 이론화」·
「부품 호환」 같은 두세 단어. 마크업은 `<span><b>연도</b>사건<i>의의</i></span>`. 의의도 그 주차 슬라이드의 설명에서 뽑는다. **연표 칸에는 `word-break:keep-all` 을 건다(2026-09-19, 교수자 요청).** 한글 기본값이면 글자 사이 아무 데서나 줄이 바뀌어 확대해서 강의할 때 「토크빌 『미국의 민주주 / 의』」처럼 낱말이 잘린다. keep-all 이면 띄어쓰기와 구분점에서만 끊긴다. **글자 크기는 건드리지 않았다** — 교수자가 화면을 확대해서 본 것이지 글자가 큰 게 아니었다. 셋 다 축소 없이 들어간다.
주차에 따라 연대 칸 단위는 바꾼다(2주차는 수백만 년~18세기라 시대 칸, 4주차는 10년 칸). 블록을 고친 본을 보여주고
확인받은 뒤 배포하고, 4주차 대본도 블록별로 같이 쓴다.

**나머지 주차는 아직 안 훑었다.** 이어서 할 때 **한 장씩 고쳐 교수자에게 확인받는
방식**으로 간다 — 한꺼번에 바꾸면 교수자 목소리와 어긋나는 데가 생긴다. 2026-09-12 에
묶음으로 열두 장을 고쳤다가 교수자가 멈추라고 해 전부 되돌린 일이 있다.
**「속도를 빠르게」는 묶음으로 처리하라는 뜻이 아니라 한 장에 걸리는 시간을 줄이라는
뜻이다.**

**고치고 나면 사실이 빠지지 않았는지 기계로 대조할 것.** 커밋 전 판과
연도·수치·고유명사 집합을 비교하면 된다. 시험 범위라 누락이 곧 사고다.

## 슬라이드 마크업 어휘

```html
<section class="slide" data-era="..." data-week="N" data-title="..." data-dates="...">
  <div class="motif" aria-hidden="true"></div>
  <div class="stage">...</div>
</section>
```

| 클래스 | 용도 |
|---|---|
| `stage--split` / `.img-left` | 텍스트+사진 2단 (기본 레이아웃) |
| `stage--hero` + `.hero-photo` | 건축물 등 **한 장 크게** (972×648로 표시) |
| `section-head has-era` + `.era-bar`/`.era-name` | 문명·시대 구분 헤더 (2주차 고대~르네상스에서 사용) |
| `bullet-list` | 본문 불릿 |
| `think-block` | `<span class="mk" role="img" aria-label="THINK">?</span><p>…</p>` — **이 구조 꼭 지킬 것**. 화면에 뜨는 것은 「THINK」 글자가 아니라 **물음표**다(덱 25곳이 모두 이 꼴). `<strong>`으로 쓰면 flex가 칼럼으로 깨진다 |
| `pullquote` + `<cite>` | 인용문 (`quote-block` 아님) |
| `compare` / `compare--photo` | 2단 대비 |
| `figure-grid` + `.figure-card.has-photo` | 3칸 카드 |
| `dual-tl` | 미술사·시기·디자인사·한국 4열 연대표 (1주차) |
| `flowmap` | 픽토그램 흐름 SVG. **현재 쓰는 슬라이드 없음** — 4페이지가 빽빽해져 도식을 뺐다. CSS는 남겨뒀으니 필요하면 되살릴 것 |
| `schedule-table` / `grade-weights` | 15주 계획 / 평가 비율 |
| `lightbox` | 사진 클릭 확대. `#lightbox` 는 **body 직속**이어야 한다 — `.slide` 안에 두면 `fitActive` 의 `scale()` 때문에 `position:fixed` 가 그 안에 갇힌다. 열려 있는 동안 좌우 키·스와이프를 막는다 |
| `carousel` | 한 칸에 사진 여러 장(2026-09-14, 61번 앙피르에 처음 씀). `<figure class="artifact-photo carousel">` 안에 `.carousel__stage` > `img`들(첫 장에 `is-on`, 각 `img` 에 `data-who`·`data-lic`), `figcaption`, `.carousel__bar`(‹ 버튼 · 점 · 「1 / 6」 · › 버튼). 조작은 버튼 · **↑ ↓ 키** · 폰에서 사진 위 가로 밀기 — **← → 와 리모컨은 그대로 슬라이드를 넘긴다.** 캡션은 사진마다 JS 가 갈아끼우고 라이트박스도 그걸 쓴다. **확대(라이트박스)한 채로도 넘긴다** — 캐러셀 사진을 누르면 좌우에 ‹ › 버튼과 「3 / 6」이 뜨고, ← → ↑ ↓ · PageUp/Down(리모컨) · 가로 밀기로 넘긴다. 슬라이드의 캐러셀도 같이 넘어가서 닫으면 마지막에 본 사진이 남는다. 캐러셀이 아닌 사진을 확대하면 버튼이 안 뜬다(2026-09-14, 교수자 요청). **`loading="lazy"` 를 쓰지 말 것** — 숨은 사진이 안 받아져 `verify.js` 가 깨진 이미지로 잡는다. 칸 높이를 고정(52vh)해 사진이 바뀌어도 `fitActive` 가 다시 줄이지 않는다 |
| `data-skip` | `<section class="slide" data-skip …>` — **시간이 밀리면 건너뛰어도 되는 장.** 오른쪽 아래 괘선(ㄱ자) 대신 작은 회색 점이 찍힌다. 학생은 모르고 교수자만 아는 표시라 일부러 눈에 안 띄게 했다. 3주차는 **59·81·83·87**(대본의 「생략 가능」과 같음, 2026-09-14 — 2주차 연표가 들어가기 전에는 58·80·82·86이었다). 4주차는 **102·103·105·106·110·115·116·119·120·123·126·130·134** 13장 — 100분 수업을 **60분**으로 줄일 때 건너뛴다(2026-09-19). 장을 지우지 않고 표식만 한다. 대본에도 장 번호 옆에 「생략」으로 적고 60분 시간표를 따로 뒀다. 장을 넣고 빼도 속성이 섹션에 붙어 있어 안 밀린다 |
| `gloss` · `gl` | **주석**(2026-09-19, 교수자 요청 — "뜬금없이 고유명사만 나오면 이해가 어렵다"). 처음 나오는 장소·기관 이름 뒤에 `<sup class="gl">*</sup>`, 장 맨 아래(map-note 다음)에 `<p class="gloss">* <b>이름</b> 원어 — 한두 줄 설명</p>`. 첫 사례 113번 말보로 하우스. **필요한 장에 그때그때 단다.** 설명은 확인한 사실만 쓴다 |
| `keywords-line` | 각 주차 타이틀 슬라이드(`stage--title`)의 `.subhead`(또는 `.era-chip`) 바로 다음 줄. `<p class="keywords-line"><b>keywords</b>단어1 · 단어2 · 단어3 · 단어4</p>` 형식, 주차당 4개. 시험 주간(8·15주)은 없음 |

**모바일 레이아웃(2026-09-06 추가).** `@media (max-width:700px)` 한 블록이다.
폰에서는 **한 화면에 한 장을 포기하고, 세로로 읽고 옆으로 넘긴다** — 16:9 를 세로 화면에 맞추면
`fitActive` 가 0.62 까지 줄여도 글씨가 안 읽혔다. 바뀌는 것은 넷이다.

- `.slide.is-active` 를 **세로 스크롤 컨테이너**로 (기본은 `overflow:hidden` 이라 잘렸다)
- `fitActive()` 가 **조기 반환**해 `scale()` 을 걸지 않는다 (CSS 에도 `transform:none !important`)
- **vh 로 묶인 사진 상한을 푼다** — 스크롤되는 문서에서 뷰포트 높이는 기준이 못 된다
- 다단은 1단으로 접고, **4열 연대표(`dual-tl`)만 가로 스크롤**로 남긴다(접으면 뜻이 사라진다)

스와이프는 **가로가 세로보다 1.5배 클 때만** 넘어간다 — 세로로 읽는 중 손가락이 옆으로 흘러
장이 멋대로 넘어가던 것을 막았다. 좌우 화살표는 모바일에서 화면 가운데 대신 **아래 구석**으로 내렸다.

**700px 초과에는 아무 영향이 없다.** 태블릿(744·820·1180)은 기존 900px 미디어쿼리로 이미
2단이 접혀서 축소도 잘림도 없다 — 확인함. `verify.js` 프로젝터 검증도 그대로 통과한다.

팔레트는 **모노톤 고정**(`--accent`가 잉크색과 같음). 색으로 강조 못 하니 크기·굵기·괘선으로 위계를 만든다.
**예외는 하나다(2026-09-14, 교수자 요청)** — 하단 진행 막대의 주차 경계 표시(`.progress__ticks i`)를
회색 세로선에서 **노란 점(`#f2c230`)** 으로 바꿨다. 회색 바탕에 회색 선이라 강의 중 안 보였다.
막대는 여전히 **덱 전체 298장** 기준이다 — 「이번 주만 채우고 25·50·75분 점」 안도 보여드렸지만
교수자가 지금 막대 그대로를 골랐다. 슬라이드 본문에는 색을 쓰지 않는 규칙은 그대로다.
다크모드 없음 — 프로젝터용으로 의도적으로 라이트 고정.

**어둡게 하자는 안은 2026-09-12 에 시험해보고 접었다.** 3주차만 `.slide[data-week="3"]` 로 토큰을
뒤집어 네 장을 찍어 보여드렸고(타이틀·재스퍼웨어·백과전서 도판·보도니 견본), 화면 자체는 나쁘지
않았다 — 캡션 띠가 흰 바탕 판화를 액자처럼 받쳐준다. **교수자가 접은 이유는 강의실 조명이다.
형광등을 켜고 수업하므로 어두운 화면은 흐려진다.** 다시 꺼내지 말 것.

**자동 맞춤**: `.slide`는 `overflow:hidden`이라 내용이 많으면 소리 없이 잘린다.
그래서 `fitActive()`가 슬라이드를 넘길 때마다 재서, 넘치면 `.stage`에 `scale()`을 걸어 통째로 줄인다(하한 0.62).
`hero-photo`는 flex로 남는 높이를 받아 **사진이 먼저 줄어들고 글자는 유지**된다.
축소는 안전망일 뿐이다 — `verify.js`가 85% 미만 축소를 "과도축소"로 알려주면 그 장은 내용을 덜어내는 게 맞다.

## 덱에서 바깥으로 나가는 링크 (2026-09-12 추가)

「지금도 판다」를 보여주려고 **66번 웨지우드**와 **94번 토넷**에 공식 사이트 링크를 달았다.
덱에 `<a>` 가 들어간 것은 이때가 처음이라 규칙을 만들었다.

- 스타일은 `.map-note a` 하나뿐이다. **모노톤이라 색 대신 밑줄**로 표시한다.
- **`target="_blank" rel="noopener"` 를 반드시 건다.** 허브 앱이 덱을 iframe 으로 띄우므로,
  이게 없으면 링크를 누르는 순간 iframe 안에서 덱이 통째로 다른 사이트가 된다.
- **수업 전에 링크가 살아 있는지 열어볼 것.** 상품 페이지 주소는 잘 바뀐다. 대본에도 적어뒀다.
- 사이트가 적어둔 연도를 그대로 믿지 말 것. 웨지우드는 「royal heritage dates back to 1763」
  (왕실 도공 임명 연도, 창업은 1759)이고, 토넷은 「200년 전통」과 「1859년부터」를 같이 쓴다
  (공방 개업 1819 / 모델 14번 1859).
- 토넷은 **오리지널 논쟁**이 있다. 가족·상표는 독일 Thonet GmbH, **미하엘 토넷이 1861년에 지은
  공장과 그 터는 체코 TON** 이다. 회사 홈페이지 문구는 앞쪽만 말한다.

## 이미지 규칙

- **웹용.** 인쇄 해상도 불필요. 긴 변 **일반 1000~1200px / hero 1500px**, WebP **장당 ~100KB** 목표(상한 170KB).
  받고 나서 용량이 넘으면 폭을 줄여 다시 받는다. 판화·선화는 같은 폭에서도 사진보다 훨씬 가볍다.
- **상한을 지킨다(2026-09-11 정리).** 넘는 8장을 그날 전부 맞췄다 — 106장 평균 91KB·합계 9.5MB,
  **170KB 초과 0장.** 세부가 많은 회화는 **품질만 낮춰선 안 맞는다**. `w02-rococo`(뷔르츠부르크
  거울의 방)는 q=80 → 48 까지 내려도 285KB 였고, **1200 → 800px 로 줄이니 q=78 에 161KB** 가 됐다.
  순서는 **폭을 먼저 줄이고 품질은 78 쯤에서 버티는 것**이 낫다. 폭만 줄이면 화면에서 멀쩡하다.
  hero 도 1500px 을 고집할 것 없다 — 실제 표시가 972×648 이라 1200px 이면 충분하다
  (`w02-hero-colosseum` 1500/q66 163KB 대신 1200/q78 126KB 를 골랐다).
- 컬러 사진 우선. 흑백은 피한다(교수자 요청).
- **잘리면 안 된다.** `object-fit:cover`는 세로 사진을 망친다 — 비율이 제각각이면 `contain`을 쓰거나 소스를 미리 같은 비율로 크롭.
- 라이선스는 CC/PD만. `fetchimg.py`가 자동 검증하고 `img_credits.tsv`에 기록한다.

```bash
python3 tools/slides/fetchimg.py <slug> "File:커먼즈파일명.jpg" 1200 4   # 마지막은 주차
```

의존성 없다. 예전 판은 Pillow 를 썼는데 시스템 파이썬이 외부 패키지 설치를 막아(PEP 668)
세션이 바뀌면 안 돌았다. 지금은 macOS 기본 `sips` 와 brew 의 `cwebp` 만 쓴다.
경로도 스크립트 위치 기준이라 레포를 옮겨도 깨지지 않는다.

`img_credits.tsv`는 74장 전체의 파일명·사용 주차·크기·라이선스·저작자·출처를 담는다.
초기 세션에서 수집한 50장은 **라이선스만 슬라이드 캡션에서 역추출**했고 커먼즈 원본 파일명이 없다.
외부 배포나 출판이 필요해지면 그 50장의 원출처부터 확인할 것.

## 배포 절차

```bash
node tools/slides/verify.js          # 1. 검증 — 반드시 통과시킬 것
git add -A public/slides && git commit && git push origin main
```

푸시하면 Vercel이 자동 배포한다. 라이브: https://design-history-app.vercel.app/slides/index.html

**덱을 고쳤으면 대본과 PDF 도 같이 간다.** 세 곳이 어긋나기 쉽다(2026-09-11 에 실제로 어긋났다).

1. 덱 `public/slides/index.html`
2. 대본 아티팩트 — 화면 문안을 인용하므로 **바꾼 장의 회색 칸을 고쳐야 한다**
3. 바탕화면 `~/Desktop/디자인사lecture/*.pdf` — 교수자가 실제로 보는 파일

대본은 아티팩트(HTML)라 PDF 가 자동으로 안 나온다. **헤드리스 크롬으로 뽑는다.**
아티팩트 본문에는 `<html><head>` 가 없으므로(발행할 때 씌워진다) 아래 껍데기를 입혀야 한다.

```bash
SRC=대본.html          # 아티팩트에 발행한 그 파일
OUT=~/Desktop/디자인사lecture/디자인사_3주차_대본.pdf
{ printf '%s' '<!doctype html><html><head><meta charset=utf8><style>:root{color-scheme:light}body{margin:0}</style><style>@page{size:A4;margin:13mm 12mm}.wrap{padding-top:0!important}.slide,.screen,.ask,.land,.aside,.thesis,.howto{break-inside:avoid}.block__head,h1,h2{break-after:avoid}</style></head><body>'
  cat "$SRC"; printf '%s' '</body></html>'; } > /tmp/print.html
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --virtual-time-budget=20000 --no-pdf-header-footer --print-to-pdf="$OUT" file:///tmp/print.html
```

`.slide{break-inside:avoid}` 가 핵심이다. 이게 없으면 한 장의 설명이 페이지 중간에서 끊겨
수업 중에 읽기 불편하다. 있으면 **번호 하나당 한 쪽**이 된다(3주차 48장 → 54쪽).
`--virtual-time-budget` 은 구글 폰트가 받아지길 기다리는 시간이다. 짧으면 폰트가 깨진다.

## 실제로 겪은 함정 (반복 금지)

1. **정규식이 다른 슬라이드를 잡는다.** 여러 슬라이드에 같은 클래스가 있으므로,
   먼저 `<section>` 블록을 격리한 뒤 그 안에서만 치환할 것. 치환 후 어서션으로 검증.
   `<ul class="bullet-list">`처럼 정확 매칭하면 `style` 속성 붙은 것을 놓친다.
2. **참조 중인 이미지를 지우지 말 것.** 지우기 전 `grep -c` 로 참조 확인.
3. **CDN 전파 지연 오탐.** 배포 직후 `curl` 이 옛 내용이나 8955바이트 HTML 폴백을 주는 일이 잦다.
   실패로 보이면 **재확인부터**. 로컬과 라이브의 **바이트 수 비교**가 가장 확실하다.
4. **파일명을 믿지 말 것.** `Terracotta-Army-Museum-01.jpg`가 병마용이 아니라 박물관 건물이었다.
   넣기 전에 이미지를 실제로 볼 것.
5. **전역 페이지 번호는 슬라이드를 추가하면 밀린다.** 교수자가 "N페이지"라 하면 현재 번호로 다시 셀 것.
6. **`slide_overrides` 는 지금 비어 있다 — 파일이 곧 화면이다.** 덱은 Supabase `slide_overrides`
   를 읽어 해당 슬라이드의 `.stage` 를 통째로 갈아끼운다. 행이 있으면 **파일만 고쳐도 화면이 안 바뀐다.**
   2026-09-06 에 남아 있던 2주차 두 건(`(2,2)` 기원과 장인정신, `(2,36)` 귀족의 인테리어)을
   **파일과 텍스트가 완전히 같은 것을 확인하고 지웠다.** 화면은 그대로다.
   백업 `tools/slides/backup/slide_overrides-20260906-151709.json`,
   복원 `tools/slides/restore_overrides.sh <백업파일>`.
   확인:
   ```bash
   KEY=$(grep -o "SUPABASE_ANON_KEY = '[^']*'" public/slides/index.html | sed "s/.*'\(.*\)'/\1/")
   URL=$(grep -o "SUPABASE_URL = '[^']*'" public/slides/index.html | sed "s/.*'\(.*\)'/\1/")
   curl -s "$URL/rest/v1/slide_overrides?select=week_id,slide_index" -H "apikey: $KEY"
   ```
   **왜 지웠나.** 오버라이드는 `(주차, 주차 안 순번)` 으로 걸린다. **슬라이드를 중간에 넣거나 빼면
   조용히 한 칸씩 밀려 엉뚱한 장을 덮어쓴다.** 1주차를 재배열하다 「미술사와 디자인사」 오버라이드가
   「15주 수업계획」 자리에 걸린 적이 있고, 2026-09-06 에 2주차 11번(핵심 문장)을 넣었을 때도
   `(2,35)` 가 「귀족의 인테리어」 내용을 「로버트 애덤」 위에 덮고 있었다. 내용이 파일과 같아
   하는 일은 없으면서 사고만 내는 상태였다.
   **다시 만들 거라면** — `/admin/slides` 로 고치면 행이 생긴다 — 그 주차의 슬라이드를 넣고 뺄 때마다
   순번을 다시 맞추거나, 파일에 반영하고 행을 지울 것. `verify.js` 가 배포 전에 DB 를 읽어
   대조하고, 순번이 밀렸거나 파일과 내용이 다르면 **실패로 막는다**(네트워크가 안 되면 건너뛴다).
7. **넘침은 스크롤이 아니라 잘림으로 나타난다.** 옛 `verify.js`는 `.stage`의 `scrollHeight-clientHeight`를 봤는데
   stage에는 높이 제약이 없어 늘 0이었다 — 10장이 잘리는 동안 "넘침 없음"으로 통과했다.
   반드시 **stage의 렌더 높이를 슬라이드 안쪽 영역과 직접 비교**할 것. `72vh` 같은 뷰포트 단위도 같은 함정이다
   (크롬바·패딩·캡션 높이를 빼지 못한다).

## 주차별 진행 상황

| 주차 | 주제 | 슬라이드 | 이미지 | 교재 반영 |
|---|---|---|---|---|
| 1 | 오리엔테이션 | 6 | 0 | — |
| 2 | 선사~18세기 | 40 | 41 | ✅ |
| 3 | 이성의 시대와 산업혁명 | 50 | 23 | ✅ 3장 전체 + 미술사 곁들임 |
| 4 | 합리화된 생산과 만국박람회 | 39 | 17 | ✅ 4·5장 + 미술사 곁들임 |
| 5 | 개혁의 바람, 미술공예운동 | 36 | 38 | ✅ 6·7장 (2026-09-19 요약 문체·보강) |
| 6 | 아르누보 | 18 | 4 | ⚠ 8장이 맞다(표에 7장으로 잘못 적혀 있었음). 매킨토시·라이트 등 7장 일부가 섞여 있다 |
| 7 | 이론에서 실천까지 | 15 | 2 | ✅ |
| 9 | 포디즘 | 15 | — | ❌ 교재 미반영 |
| 10 | 모더니즘·바우하우스·신 타이포그래피 | 21 | 2 | ❌ 교재 미반영 |
| 11 | 아르데코 | 15 | — | ❌ 교재 미반영 |
| 12 | 굿 디자인·스위스 타이포그래피 | 19 | 3 | ❌ 교재 미반영 |
| 13~14 | 한국 근현대 · 디지털·AI | 각 15 | — | ❌ 교재 미반영 |

전체 298장. (2026-09-06 — 2주차에 핵심 문장 1장, 10주차에 신 타이포그래피 6장, 12주차에 스위스 타이포그래피 4장 추가.
2026-09-07 — 4주차에 「오웬 존스와 수정궁의 색」·「누가 박람회를 봤나」 2장 추가, 기존 6장 보강, 이미지 4장 추가.
2026-09-09 — 3주차를 25 → 48장으로. 시대배경 2장 + 미술사 곁들임 20장 + 「신고전주의라는 말」 1장, 이미지 9장 추가.
2026-09-11 — 4주차 문안을 3주차 기준으로 정리하고, 미술사 곁들임 8장 추가로 31 → 39장, 이미지 5장 추가)

**3주차에 미술사를 섞은 방침(2026-09-09).** 교수자 요청이다. 미술사는 1학기 과목이고 이 수업은 디자인사다.
그래서 **미술사가 주제가 되는 슬라이드는 두지 않는다** — 미술사는 언제나 **디자인사의 한 대목을 설명하는 도구**로만
들어간다. 예: 폼페이 발굴은 "고고학이 양식의 원료를 공급했다"로, 버크의 숭고는 "그래서 다음 세대가 용광로를
그릴 수 있었다"로, 다비드는 "신고전주의가 정치 언어였다"로 착지한다. 마지막 장 「같은 시대, 두 개의 이름」이
이 방침 자체를 학생에게 설명한다. **새 미술사 슬라이드를 넣을 때도 착지점을 디자인사에 둘 것.**

**양식 이름은 말뜻부터 푼다.** 1학년은 선행 이론과목이 없다. 3주차는
「신고전주의라는 말」에서 신(neo)+고전(classical)을 뜯고 로코코와 형태를 대조한 뒤에야
폼페이·그랜드투어·양식 순서로 간다. **다른 주차에서 새 양식 이름을 처음 꺼낼 때도 같이 할 것.**

**시대배경은 맨 앞에 둔다.** 예전 3주차는 계몽주의 3불릿으로 바로 시작해 1학년이 발 디딜 데가 없었다.
지금은 「1750년의 세계」(인구·동력·속도·주문생산)와 「한 세대에 겹친 세 개의 혁명」이 먼저 나온다.

**슬라이드 밀도(2026-09-11).** 교수자가 수업에서 서술하므로 **화면은 항목만 남긴다.**
서술어와 접속구를 걷어내고 나란한 것은 줄을 가른다. 장당 글자 수(태그 제외)가 기준이다 —
2주차 313 · 3주차 293 · 4주차 318. **400자를 넘으면 서술이 남아 있는 것이다.**
재는 법: `slides.map(s => s.replace(/<[^>]+>/g,' ').trim().length)` 의 주차별 평균.

**4주차에 미술사를 섞은 방침(2026-09-11).** 3주차와 같은 원칙이다. 교수자가 고른 갈래는
**살롱·사실주의·1855 파리 만국박람회** 하나뿐이고, 8장을 한 덩어리로 「새로운 소비자의 등장」 뒤,
미국식 생산 앞에 넣었다. 착지점은 전부 디자인사다 — 살롱의 주제 위계는 "헨리 콜의 박물관과 같은 일을
하고 있었다"로, 쿠르베의 낙선과 사실주의 관은 "승인 대신 전시를 직접 만든 사건"으로,
마지막 「박람회는 무엇을 승인했나」가 공포의 방(가르치기) ↔ 낙선(거르기)으로 묶는다.
**「사실주의라는 말」에서 real+-ism 을 먼저 푼다** — 양식 이름은 말뜻부터 푸는 규칙 그대로다.

**다음 할 일**
0-1. **3주차 문제 후보 10개를 뽑아뒀다(2026-09-12).** `~/Desktop/디자인사lecture/디자인사_3주차_문제후보.pdf`
   5쪽. 기존 「중간고사 문제안」 아티팩트에는 3주차 문항이 셋뿐이라(핀 공장·휘트워스·토넷) 겹치지 않게
   만들었다. **전부 넣으면 3주차가 13문이 되어 균형이 깨지니 6~7개만 고를 것.** 교수자 검토 대기.
0. **4주차 대본이 아직 없다.** 덱은 2026-09-11 에 39장으로 정리를 마쳤다(전역 **100~137**, 2026-09-14 기준 — 4주차 블록 1 개편과 2·3주차 연표로 번호가 여러 번 움직였다).
   3주차 대본이 형식의 본보기다. 100분 기준으로 짜면 된다.
   덱 문안 정리는 끝났다 — 볼드 불릿당 0.94(2개 이상 0개), 장당 평균 318자.
   **화면에서 줄이며 대본으로 내린 것**: 오웬 존스가 감독한 두 부문의 정식 명칭
   ("종이·인쇄술·제본술", "조각·모델·조형미술·모자이크·에나멜"), 콜트의 마케팅 항목 나열
   (판촉·홍보·제품표본화·광고·공보활동), 콜트가 화학실험으로 관객을 웃기던 이력.
1. **5~7주차 이미지 보강.** (4주차는 2026-09-07 에 12장까지 채웠다) 3주차를 8/25 → 15/25 로 채운 방식이 기준이다.
   7주차가 가장 비어 있다(15장에 2장) — 베렌스의 AEG 제품·서체, 로스의 건축은 그림이 있어야 한다.
1-1. (완료 2026-09-11) **이미지 8장이 상한 170KB 를 넘었다.** 106장 평균 96KB·합계 10MB 로 전체는 가볍다.
   넘는 것은 `w02-rococo`(334KB) · `w09-01-042`(227) · `w02-baroque-peterskirche`(216) ·
   `w02-renaissance-vitruvian`(203) · `w02-hero-pavilion`(201) · `w02-hero-chartres`(180) ·
   `w05-01-029`(168) · `w02-hero-colosseum`(168). 세부가 많은 회화라 **q=80 → 48 까지 낮춰도
   285KB 에 머무는 것이 있다** — 품질만으로는 안 되고 폭을 줄여야 한다.
   폭을 줄여 전부 맞췄다. 자세한 것은 위 「이미지 규칙」.
2. 8장 이후 교재 PDF 를 받아 9주차 이후 보강. 바탕화면 「디자인의역사」 폴더에는 7장까지만 있다.
3. 학생 모달이 덱을 `clean=1` 없이 띄워, **닫힌 주차까지 '주차 이동' 메뉴로 열람 가능**하다.
   `components/LectureMaterialButton.jsx` 의 `src` 한 줄 문제인데 다른 세션이 그 파일을 작업 중이라 미뤄뒀다.

## 곁들여 만든 문서

슬라이드 밖에 있는 것들. 덱이 바뀌면 이쪽도 어긋난다.

| 문서 | 주소 |
|---|---|
| 오리엔테이션 대본 (8장·25~27분) | claude.ai/code/artifact/4c1b2f33-35d0-453b-8e16-45adaff909f2 |
| 2주차 대본 (40장·100분) | claude.ai/code/artifact/274d5477-dc3b-48c3-8080-5b02ba019ae9 |
| 3주차 대본 (50장·100분) — 2026-09-11 갱신 | claude.ai/code/artifact/768d373f-17e8-4ef2-9825-736f8ce0864b |
| 4주차 대본 (38장·100분, 덱 100~137) — 2026-09-14 작성, 2026-09-19 124~137 말하는 부분 다시 씀. 블록 2~4 화면을 요약 문체로 바꾸면 회색 칸을 덱에서 다시 뽑는다 | claude.ai/code/artifact/c1d10e85-3785-4337-be85-56597e5ff094 |
| 중간고사 문제안 (1~7주차) | claude.ai/code/artifact/36454fe4-b2bc-44cd-b7a7-cd2914b55fa6 |
| 5주차 대본 (36장·100분, 덱 138~173, 60분 표식 11장) — 2026-09-19 작성, 같은 날 말하는 부분 다시 씀 | claude.ai/artifact/9hv5PiDgaSPK1kCuA95ZBM |

대본은 **덱의 전역 번호**로 장을 가리킨다. 슬라이드를 넣거나 빼면 번호가 밀려 대본이 어긋나므로,
**덱을 고쳤으면 그 주차 대본의 번호도 같이 확인할 것.** 3주차 대본은 지금 **50~99번** 기준이다(2026-09-14 에 97번 연표를 넣어 브릿지가 98번이 됐고, 같은 날 2주차 끝에 연표 한 장이 들어가 전체가 다시 한 칸 밀렸다).

대본 문체는 **합니다체**(소리 내어 말하는 글이라 슬라이드의 `~하다`체와 다르다).

**대본의 말하는 부분은 외부 모델 초안을 거친다(2026-09-19, 교수자 지시 — "글이 딱딱하다").** 4·5주차(124~173번)를 ChatGPT(gpt-5.5)로 다시 썼다. 도구는 레포 밖 `~/design-history-backup/대본빌더/draft.py`(키 `~/.config/openai/key`), 검토·빌드 스크립트는 같은 폴더 `review/`. 원칙은 넷이다.
- **초안을 그대로 넣지 않는다.** 이전 판과 장마다 숫자·「」·『』를 기계로 대조하고(`review/newfacts.py`), 새 사실은 근거가 확실할 때만 남긴다.
- **말하는 부분만 바꾼다.** 회색 칸(화면)·회색 작은 칸·보라색 칸·「60분이면」 문장은 그대로 둔다. 화면 문안 초안은 연도 줄을 합치거나 2단 장을 불릿으로 푸는 등 덱 규칙을 어겨서 쓰지 않았다 — **덱은 이번에 바뀌지 않았다.**
- 초안이 자주 남기는 것 — 교수 메모를 소리 내 읽게 쓴 문장(「시간이 밀리면 이 장은…」), 내부 제목을 읽는 예고(「다음은 「X」입니다」), 라이선스 읽기, 「~인 셈입니다」. 빌더가 앞의 둘을 자동으로 뺀다.
- 물음표 칸(`.ask`)은 flex 라 `<p>` 가 둘이면 두 단으로 갈라진다. 한 문단으로 합칠 것.
모델 호출은 교수자가 「돌려」라고 할 때 모아서 한 번에 한다. 새 OpenAI 계정은 분당 요청 3건·토큰 1만이라 `draft.py a b` 로 순서대로 돌린다(재시도 내장, 한 장에 약 1분).
학생 전공을 대본에 적지 말고, **억지로 학생 작업에 갖다 붙이지 말 것** — 사실만 쓴다.
