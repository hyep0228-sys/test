-- =========================================================
-- 팀 논의 주제 — 교수자가 그 주차에 무엇을 논의할지 적어 둔다
-- =========================================================
--
-- 주차마다 하나뿐이고 논의를 여는 것과 짝이라, 별도 테이블 없이
-- discussion_open 옆에 컬럼으로 붙인다.
-- 쓰기 권한은 이미 있는 "weeks: professor writes"(for all) 가 그대로 덮는다.

alter table public.weeks
  add column discussion_topic text;
